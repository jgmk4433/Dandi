# ---------------------------------------------------------
# 필수 패키지 (pip install -r requirements.txt)
# 별도 서버 프로그램(Redis, PostgreSQL, 클라우드 서비스) 설치가 필요 없다.
# 이미지 해상도 확인도 표준 라이브러리로 처리하므로 Pillow 가 필요 없다.
# ---------------------------------------------------------
fastapi>=0.110
uvicorn>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
python-multipart>=0.0.9   # 이미지 multipart 업로드 처리용
PySide6>=6.6              # 모니터 UI (--no-ui 로 실행하면 불필요)

# ---------------------------------------------------------
# 선택: 허브 YOLO 재검증을 켤 때만 설치
# ---------------------------------------------------------
# ultralytics>=8.3
# opencv-python-headless

# ---------------------------------------------------------
# 제거됨: celery, redis
#   -> 외부 브로커 설치 없이 구동해야 하므로 DB 기반 내장 큐로 대체했다.
# ---------------------------------------------------------
