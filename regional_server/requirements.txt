# ---------------------------------------------------------
# 지역 서버 필수 패키지
#   pip install -r requirements.txt
#
# VLM 은 Ollama(별도 설치)를 HTTP 로 호출하므로 torch 등 무거운 패키지가 필요 없다.
# ---------------------------------------------------------
fastapi>=0.110
uvicorn>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
pillow>=10.0          # 이미지 크롭/업스케일

# ---------------------------------------------------------
# 별도 설치 필요 (pip 아님)
#   1) Ollama        : https://ollama.com/download
#   2) 모델 내려받기 : ollama pull qwen3-vl:7b
# ---------------------------------------------------------
